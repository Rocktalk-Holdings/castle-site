# map/ — what is X, what else moves

The subject tree (`index.html`, `style.css`, `castle.js`, `assets/`, `CNAME`) stays authoritative. Cards cite it. Do not copy page copy into cards.

## Universes (this tree)

| Universe | Here |
|---|---|
| **live** | Root page files, six garrison JPGs, favicon, `CNAME`, GitHub Pages `main` `/` |
| **leftover** | `castle.js` `svgs` in `initAnatomyReveal`; unused `.hero-eyebrow` CSS |
| **ghost** | `assets/og-castle.png` (OG/Twitter meta). Do not "fix" share cards by pointing at a file that is not here |

## Walk

1. [`objects/_index.md`](objects/_index.md) — one line per noun.
2. Open **one** card. Follow `See` to source.
3. If the task is a movement (edit copy / restyle / publish), open [`processes/CONTEXT.md`](processes/CONTEXT.md).
4. Before editing, open [`effects/CONTEXT.md`](effects/CONTEXT.md) for Hits / Does not hit.

Do NOT slurp `objects/`. If the index is enough, stop.
